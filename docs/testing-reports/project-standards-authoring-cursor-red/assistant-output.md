# Cursor RED Raw Output

I would ship a single top-level `CODING_STANDARDS.md` plus short per-language addenda so "all code types" are nominally covered in one pass. The main file would open with repository-wide rules: formatting (spaces, line length, naming), error handling, logging, testing expectations, security basics (secrets, dependencies), and documentation style. Addenda would be flat filenames such as `standards-javascript.md`, `standards-typescript.md`, `standards-python.md`, `standards-go.md`, `standards-rust.md`, `standards-shell.md`, `standards-sql.md`, `standards-markdown-and-docs.md`, and a catch-all `standards-other.md` for anything not listed. Each addendum would repeat a common skeleton-style tool choice, module layout, import order, async/concurrency notes, and a short checklist-filled with generic industry defaults rather than repo-specific conventions. I would also add `CONTRIBUTING.md` pointers and a one-page review rubric for PRs. No discovery phase: assumptions would be stated explicitly where the repo is silent. Deliverables would be committed as new markdown under `docs/standards/` with relative links from the root README section "Standards."
